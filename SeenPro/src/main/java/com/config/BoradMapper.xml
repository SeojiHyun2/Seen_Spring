<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoradMapper">

	<select id="boardStage" resultType="boardStageDTO">
		select 	FREE_BOARD_NUM, FREE_BOARD_TITLE, FREE_BOARD_AUTHOR, FREE_BOARD_CONTENT, FREE_BOARD_VIEWS, FREE_BOARD_WRITEDAY
		from 	FREE_BOARD
		ORDER BY FREE_BOARD_NUM desc
	</select>
	
	<select id="totalCount" resultType="int">
		select count(*)
		from FREE_BOARD	
	</select>
	
</mapper>